extends layout

block content
  .sidebar
    block sidebar
      |<a href='/catalog'>
      |  <img alt="ncssm" src='/images/ncssm.png'
      |  width=75" height="75">
      |</a>
  center
    h1= title

  ul
  each user in user_list
    center
      p #{user.username}:  #{user.score}
  else
    li There are no users.
